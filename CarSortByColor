package com.company;

public class CarSortByColor implements Comparable<Car> {

    public int compareTo(Car c1,Car c2) {
        return c1.Color.compareTo(c2.Color);
    }

    @Override
    public int compareTo(Car o) {
        return 0;
    }
}
